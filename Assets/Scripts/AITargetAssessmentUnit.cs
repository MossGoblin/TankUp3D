using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Receives object data from AIScanner and crunches it, then sends it to AIStateMaster
/// </summary>
public class AITargetAssessmentUnit : MonoBehaviour
{
    // 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
